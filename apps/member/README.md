# Paryx Member

Reserved for the separate golfer-facing application.

The current platform foundation intentionally does not copy the golfer home, booking, profile or scorecard UI into this folder. Those features will be rebuilt/ported here later against shared Paryx services.
